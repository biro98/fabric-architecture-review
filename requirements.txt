# Cap azure-identity below 1.19: newer releases require azure-core>=1.31
# (AccessTokenInfo), which is newer than the azure-core baked into the Fabric
# Spark runtime and causes "cannot import name 'AccessTokenInfo'" at import time.
azure-identity>=1.17.0,<1.19.0
requests>=2.32.0
pyyaml>=6.0.1
jinja2>=3.1.3
markdown>=3.6
python-dotenv>=1.0.1
